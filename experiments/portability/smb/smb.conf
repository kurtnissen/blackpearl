[global]
   server role = standalone server
   security = user
   map to guest = Bad User
   server min protocol = SMB2
   smb ports = 445
   logging = stdout
   log level = 1

[blackpearl]
   path = /data
   browseable = yes
   read only = yes
   guest ok = yes
   guest only = yes
   force user = nobody
